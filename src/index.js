let nomeHeroi = "Lucas da Costa";
let niveisXP = [10.0000]

for(contador of niveisXP){
    switch(contador){
        case 999:
            console.log(" O Herói de nome " + nomeHeroi + "está no nível " + contador);
    
        case 1.000:
            console.log(" O Herói de nome " + nomeHeroi + "está no nível " + contador);
    }      
}